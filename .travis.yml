language: ruby
cache: bundler
script: bundle
deploy:
  - provider: script
    script: bundle exec rake stage
    on:
      branches:
        except: master
  - provider: script
    script: bundle exec rake release
    on:
      branch: master
