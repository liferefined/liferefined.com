language: ruby
cache: bundler
script: bundle
deploy:
  - provider: script
    script: bundle exec rake stage
    on:
      all_branches: true
      condition: $TRAVIS_BRANCH != master
  - provider: script
    script: bundle exec rake release
    on:
      branch: master
